# Instructions

To run the webiste locally, you need to follow command lines:
```
    bundle init  # to generate Gemfile
    bundle add jekyll  # to add jekyll in
    bundle exec jekyll serve  # to run locally
```